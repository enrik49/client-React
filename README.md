First of all clone de repository

# Install dependencies

Now we need to download the dependencies, execute the follow command

### `npm install`

## Run the client
And the last step is run the client

### `npm start`
